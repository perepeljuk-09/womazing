import {MainContainer} from "../utils/mainContainer/mainContainer";

const NotFoundPage = () => {
    return (
        <MainContainer title={"Страница не найдена"}>
        </MainContainer>
    );
};

export {NotFoundPage};